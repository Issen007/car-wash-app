apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "carwashapp.fullname" . }}-test-connection"
  labels:
    {{- include "carwashapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "carwashapp.fullname" . }}:{{ .Values.carwashapp.frontend.service.port }}']
  restartPolicy: Never
